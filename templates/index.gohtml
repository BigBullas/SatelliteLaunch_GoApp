<!DOCTYPE html>
<html>
<head>
<title>Восточный</title>
<link href="https://fonts.googleapis.com/css?family=Montserrat:100" rel="stylesheet">
<link href="https://fonts.cdnfonts.com/css/st-simplesquare" rel="stylesheet">
<link rel="stylesheet" href="/style/index.css">
</head>
<body>
  <div class="header">
    <header>
      <div class="header-icon_container">
        <div class="roscosmos_icon"></div>
        <div>Роскосмос</div>
      </div>
      <div class="header-navbar_container">
        <div>Главная</div>
        <div>Услуги</div>
        <div>Галерея</div>
        <div>Контакты</div>
      </div>
      <div class="header-user_container">
        <div>Иконка</div>
        <div>Алик Нигматуллин</div>
      </div>
    </header>

    <div class="header-title">
      Космодром Восточный
    </div>

    <div class="header-subtitle">
      Один из самых масштабных проектов России ХХI века
    </div>
  </div>
  <div class="search_container">
    <form action="/home">
      <input type="text" name="spacecraft" placeholder="Введите название" value="{{ .spacecraft }}">
      <input type="submit" value="Найти">
    </form>
    {{if .count }}
      <div class="btn-draft">
          В полёте: {{ .count }} КА
      </div>
    {{ else }}
      <div class="btn-draft__disabled">
          В полёте: {{ .count }} КА
      </div>
    {{ end }}
  </div>
  <div class="card_container">
    {{if .cards }}
      {{ range .cards }}
        <div id ="{{ .PayloadId }}" class="card">
          <div class="card-img_container">
            <div class="card-img" style="background-image: url({{ .ImgURL }});"></div>
          </div>
          <div class="card-content">
            <div class="card-text_container">
              <h3>
                {{ .Title }}
              </h3>
              <div>
                <div>
                  Описание: <span>{{ .Description }}</span>
                </div>
                <div>
                  Грузоподъёмность: <span>{{ .LoadCapacity }} тонн</span>
                </div>
                <div>
                  Планируемая дата полёта: от <span>{{ .FlightDateStart }}</span> до <span>{{ .FlightDateEnd }}</span> 
                </div>
                <div>
                  Цена услуги: <span>{{ .DesiredPrice }} млн рублей</span>
                </div>
              </div>
            </div>
            <div class="card-btn_container">
              <form action="/payload">
                <input type="hidden" name="cardId" value="{{ .PayloadId }}">
                <button class="card-button">Подробнее о КА</button>
              </form>
              <form action="http://localhost:8080/payload/{{ .PayloadId }}" method="post">
                <input type="hidden" name="cardId" value="{{ .PayloadId }}">
                <button class="card-button">Удалить КА</button>
              </form>
              <form action="http://localhost:8080/add_to_draft/{{ .PayloadId }}" method="post">
                <input type="hidden" name="cardId" value="{{ .PayloadId }}">
                <button class="card-button">Добавить в полёт</button>
              </form>
            </div>
          </div>
        </div>
      {{end}}
    {{else}}
      <div>
        <h2>Ничего не найдено</h2>
      </div>
    {{end}}
  </div>
  <footer>Как связаться с нами</footer>
</body>
</html>